pipeline {
    agent any

    parameters {
        string(defaultValue: env.GIT_BRANCH, description: 'This is my dynamic branch', name: 'MY_BRANCH')
    }

    stages {
        stage('checkout') {
            steps {
                echo 'hello'
                
            }
        }
    }
}
