pipeline {
    agent any
     environment {
        DEFAULT_BRANCH = env.GIT_BRANCH
    }
    stages {      
         stage('Clone Repo') {
             steps{
           echo env.GIT_BRANCH
             }
    
        }
        
    }
}





