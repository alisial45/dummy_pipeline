pipeline {
    agent any


    stages {
        stage('Hello') {
            steps {
                script {
                    
                    echo "Building branch: ${env.BRANCH_NAME}"
                    // Add steps to build the specified branch
                }
            }
        }
    }
    
}







